interface:
  display_name: "Aiope"
  short_description: "Repo-specific patterns and workflows for aiope"
  default_prompt: "Use the aiope repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true